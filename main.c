#include <stdio.h>
#include <stdlib.h>

int main(){

    char palavraSecreta[20];
    int acertou=0, enforcou=0;

    sprintf(palavraSecreta, "MELANCIA");

    do {
        //começar o jogo!

    } while (!acertou && !enforcou);

    return 0;
}